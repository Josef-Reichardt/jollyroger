env:
  APP_URL: "https://ninja.example.com"
  APP_DEBUG: "false"
  REQUIRE_HTTPS: "true"
  TRUSTED_PROXIES: "*"
  PDF_GENERATOR: "snappdf"

  # Mail (Pflichtfelder, sonst schlägt Laravel-Bootstrap fehl)
  MAIL_MAILER: "log"     # "log" = kein echter Mailserver nötig zum Starten
  MAIL_HOST: ""
  MAIL_PORT: "2525"
  MAIL_USERNAME: ""
  MAIL_PASSWORD: ""
  MAIL_ENCRYPTION: ""
  MAIL_FROM_ADDRESS: "ninja@example.com"
  MAIL_FROM_NAME: "Invoice Ninja"

  # Initiales Admin-Konto – nach erstem Start entfernen!
  IN_USER_EMAIL: ""
  IN_PASSWORD: ""

secret:
  APP_KEY: "base64:CHANGEME="
  DB_PASSWORD: "changeme"
  MYSQL_ROOT_PASSWORD: "changeme_root"

mysql:
  persistence:
    storageClass: "local-path"
    size: 10Gi

redis:
  persistence:
    storageClass: "local-path"
    size: 1Gi

persistence:
  public:
    storageClass: "local-path"
    size: 5Gi
  storage:
    storageClass: "local-path"
    size: 10Gi

ingress:
  className: "nginx"
  annotations:
    cert-manager.io/cluster-issuer: jollyroger-letsencrypt-issuer
  host: ninja-jollyroger.duckdns.org
  tls:
    enabled: true
    secretName: invoiceninja-tls
